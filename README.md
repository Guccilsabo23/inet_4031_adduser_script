Description Section:

This is a code that makes new users using pything through an adduser command.Essentially this code was created for helping manage as well as create new users



Operation Section:

To run the code you would have to take a couple steps in order for a user to run the code

1.You would have to save the file under a .py file on a terminal
2.Look in a terminal and try to find the directory where that .py is saved
3.Make sure its runnable by using the command chomad +x new_user.py
4.Finally run the script
